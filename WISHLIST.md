# SigmaLink — Wishlist

> **Capture inbox for future / nice-to-have / explicitly-deferred items.** Low ceremony.
> Promote an item into [ROADMAP.md](ROADMAP.md) when it gets scoped into a phase.
>
> Buckets: **Deferred by design** (consciously out of scope) and **Future enhancements**
> (planned-later upgrades). **New ideas** is the untriaged inbox.

---

## 🚫 Deferred by design (out of scope for now)

_(consciously NOT built — each is a separate track or a non-goal, not a gap)_

- **[desktop-runtime] Tauri migration for host-shell footprint** — replacing Electron with Tauri may reduce baseline app overhead, but the current RAM bottleneck is per-pane Claude/Codex/MCP process trees. Defer until shared MCP, lazy resume, and process-tree cleanup prove the remaining host overhead is worth a platform migration.

---

## ✨ Future enhancements (planned-later upgrades)

_(real upgrades to build once the current system is production-grade)_

- **[pane-lifecycle] automatic idle suspend for completed agents** — park idle/completed panes by preserving scrollback/session metadata and killing the live process tree. Build after the shared Ruflo HTTP and explicit lazy-resume path is stable, because automatic suspension needs clear pinned/keep-alive semantics.
- **[pane-lifecycle] pinned keep-alive policy for remote/autopilot agents** — mark panes that must never be parked by idle cleanup, especially Telegram/remote/autopilot lanes. Build when idle suspend is implemented.
- **[pane-lifecycle] Pane Hibernate: pause/resume-like RAM offload for inactive finished panes** — make the current "inactive/blurred/resuming" pane concept into a real memory feature: keep the pane visible as a cheap frozen shell, but stop the provider/MCP process tree so idle Claude/Codex panes no longer keep 300 MB-1 GB resident.
  Proposed behavior: add a `sleeping`/`hibernated` pane runtime state that preserves `sessionId`, provider, cwd/worktree, branch, runtime profile, external provider session id, scrollback snapshot, last RSS/process breakdown, and the visible pane card. On sleep, stop the PTY process tree and dispose or downgrade the renderer xterm cache; on resume, respawn the provider through the existing resume path and restore the pane to `running`.
  Why it matters: CSS blur/minimize only changes presentation and does not reduce Claude/MCP RSS. Raw OS pause (`SIGSTOP`/`SIGCONT`) is tempting but does not reliably free memory and may break network streams/MCP children. Hibernate is the reliable RAM win because it removes the live process tree until the user needs the pane again.
  Expected impact: in an 8-pane workspace where idle Claude panes average ~500 MB RSS, hibernating 6 finished panes can reduce provider-process RSS from roughly ~4 GB to roughly the 2 active panes plus lightweight pane metadata/scrollback. MCP-heavy panes benefit more because child MCP servers are terminated too.
  Likely touchpoints: `app/src/main/core/pty/registry.ts:401` and `app/src/main/core/pty/registry.ts:411` already expose tree-aware kill/stop; add a hibernate-specific stop path that persists a sleeping state instead of treating the pane as removed. `app/src/renderer/lib/terminal-cache.ts:31` and `app/src/renderer/lib/terminal-cache.ts:367` distinguish parking vs destroy; sleeping panes should probably destroy live xterm/subscriptions after persisting scrollback, while normal workspace switches still park. `app/src/main/core/pty/resume-launcher.ts:460` already supports subset resume by session id; reuse/extend it for explicit pane wake. `app/src/renderer/features/command-room/CommandRoom.tsx:294` and `app/src/renderer/features/command-room/CommandRoom.tsx:301` currently only remove or stop; add Sleep/Wake actions and a header badge.
  Safety policy: manual "Sleep pane" first; later auto-sleep only when there is a clear finished/idle signal, no active prompt, no recent output, low CPU, and the pane is not pinned keep-alive. Never auto-sleep remote/autopilot/Telegram or explicitly pinned panes. On wake failure, keep the frozen pane and show a resumable error rather than deleting the session.
  Trigger: build after the current RAM brake and strict MCP launch defaults have been dogfooded, and after we can reliably persist/restore scrollback for sleeping panes. Effort: L.

- **[pane-rendering] pane terminal text-reflow on resize — still not fully finished** — PR #133 (`66ffce4` restored `_renderService.clear()` via `fit.fit()`; `45a558e` suppressed the ResizeObserver refit during a divider drag) killed the ghosting + the mid-drag SIGWINCH storm, and the operator confirmed it "working fine for now" — but it is NOT fully polished. Residuals, each with a cited deferred fix from the 4-agent root-cause sweep (plan: `app/docs/superpowers/plans/2026-06-09-pane-content-reflow-keystone.md`):
  - 🐞 **[low] coalescer mixes old/new-width bytes around a resize + double-writes the snapshot on first mount.** `app/src/main/rpc-router.ts:989` (resize handler) and the `pty.snapshot` handler never call `ptyDataCoalescer.flush(sessionId)` (`app/src/main/core/pty/pty-data-coalescer.ts:66`). Fix: flush before `pty.resize` and before `pty.snapshot`. Effort: S.
  - 🐞 **[low] `reflowCursorLine` defaults false** → the cursor line is skipped during a column-shrink reflow → cursor desync/overlap while a TUI streams. `app/src/renderer/lib/terminal-cache.ts` `buildTerminalOptions`: set `reflowCursorLine: true` — but it has side-effects on cursor-redraw programs, so test against Claude Code's prompt first. Effort: S, risk: M.
  - **1-frame repaint flicker at release for a full-screen TUI** — the CLI's own redraw after SIGWINCH; likely NOT fixable our side (accept as inherent unless a paint-coalescing trick is found).
  Trigger: build when resize text-rendering roughness is reported again, or before claiming the pane terminal "fully polished." Severity: low. Effort: S–M.

---

## 🆕 New ideas (untriaged)

_(raw ideas land here; promote to ROADMAP.md once scoped into a phase)_

- **[mcp] detect repeated stdio MCP starts under one Codex/Claude pane** — a single pane can accumulate multiple `@claude-flow/cli mcp start` descendants. Investigate whether this is Codex plugin behavior, config reload behavior, or a leaked child cleanup issue once diagnostics can show child process history.

- 🐞 **[medium] `+ Pane` dead on a resumed/restored workspace — swarm comes back `paused`** — after closing the app and reopening (workspaces auto-resumed), the restored workspace's swarm has `status !== 'running'`, so `+ Pane` is gated with the pill *"Swarm is paused — resume it to add panes"* and you cannot add a pane without manually resuming the swarm first. Gate: `app/src/renderer/features/command-room/AddPaneButton.tsx:74-76` (`if (activeSwarm.status !== 'running') → disabledReason`). Root question to settle when scoped: should the workspace-restore/boot path set the swarm back to `running` when its panes are live, OR should `addPane()` auto-resume a paused swarm on click (it already auto-creates one when none exists, `AddPaneButton.tsx:201`)? Investigate the boot resume/swarm-load path that leaves `swarm.status='paused'` (swarm loader in `use-live-events.ts` / session-restore). Severity: medium (workaround: manually resume the swarm). Effort: S–M.

- 🐞 **[high] Jorvis `launch_pane` tool spawns panes that never appear in the Command Room** — Jorvis reports success and even names the new panes (codex/claude builders), but the grid keeps showing only the pre-existing pane. Root cause = **a missing renderer-refresh signal**: the assistant's `launch_pane` MCP tool spawns the PTY + DB rows via `executeLaunchPlan` but **emits no event** (`app/src/main/core/assistant/tools.ts:283-300` — returns `sessionIds`/`sessions`, no broadcast). The Command Room only adds spawned panes to the visible grid when it receives `assistant:dispatch-echo` (`app/src/renderer/features/jorvis-assistant/use-jorvis-dispatch-echo.ts:35` → refetch `panes.listForWorkspace` + `ADD_SESSIONS` at :76-80). The controller's `dispatchPane`/`dispatchBulk` paths DO emit that echo (`app/src/main/core/assistant/controller.ts:466` and `:620`), so panes launched via those show up — but the bare `launch_pane` tool (the one the model actually calls) doesn't. So the backend succeeds (worktree + session insert + PTY spawn) while the renderer never learns → panes invisible. Sibling-twin trap: `launch_pane` is the un-echoed twin of `dispatchPane`/`dispatchBulk` [[grep-sibling-call-sites]]. Fix direction: (A) emit `assistant:dispatch-echo` per session from the `launch_pane` handler — needs `emit` threaded into `ToolContext` (`tools.ts` ~line 39-92 has no `emit` today); OR (B) route the tool through the existing `dispatchPane`/`dispatchBulk` controller path that already echoes correctly (cleaner — single emit site). Confirm no other tool consumer depends on the bare path. Also sanity-check swarm association: if `launch_pane` spawns a swarm-less PTY, a grid that iterates swarm-agent rosters wouldn't show it either. Severity: high (core Jorvis orchestration silently no-ops in the UI). Effort: S–M. Found 2026-06-10 testing the `feat/bsp-pane-tiling` branch.

- 🐞 **[medium] dropping/pasting a screenshot into a terminal pane never reaches the agent as an image** — drop a screenshot → a workspace-relative file-path string lands in the prompt (the `…/var/folders/…` "var pwd" reference the operator saw); paste a screenshot → **nothing happens at all**. Image-capable CLI agents (Claude Code, Codex) never see the image. **Verified root cause (3-agent sweep 2026-06-10, /systematic-debugging):**
  - **Drop:** `app/src/renderer/features/command-room/PaneShell.tsx:256-321` `handleDrop` DOES fire (registered on the pane-body div at `:444` / `onDrop` ~`:457`). Its external/Finder-drop fallback (`:303-321`) treats **every** dropped `File` identically — `window.sigma.getPathForFile(file)` (preload `electron/preload.ts:30-35` → `webUtils.getPathForFile`) → `pathRelative(abs, workspaceRoot)` → writes `@<relPath> ` into the PTY via `insertMention` (`insertMention.ts:21` → `rpc.pty.write`, channel `rpc-channels.ts:28` → `rpc-router.ts:986-988` → `registry.ts:386-388`). **No MIME check** before the path write, so an `image/*` file becomes a text mention and the bytes are never read; for a screenshot living OUTSIDE the workspace the relative path degrades to a `/var/folders/…` / `../../..` reference that's fragile + not recognized as an image.
  - **Paste:** xterm's `handlePasteEvent` (`@xterm/xterm` `Clipboard.ts:43-49`) reads **only** `clipboardData.getData('text/plain')` and ignores `image/png`/`tiff`. A macOS screenshot (Cmd+Ctrl+Shift+4) is `image/png` with no text → `getData` returns `""` → `paste("")` → `terminal-cache.ts:287-291` `onData` early-returns on `clean===''` → **silently swallowed**. No app-level `paste` listener exists anywhere (Terminal.tsx / PaneShell.tsx).
  - **Decisive CLI constraint (research, cited):** both Claude Code & Codex read images **CLI-side directly from the system clipboard via Ctrl+V** (NOT Cmd+V); the PTY/terminal is a pure text pipe — image bytes never travel through it, so xterm.js fundamentally can't forward a pasted image. BUT **both CLIs also accept an image FILE PATH** (Claude detects image paths in the prompt; Codex `-i`/path). ⚠️ The "write image to the system clipboard via Electron `clipboard.writeImage`" idea is **broken for Claude Code**: Electron writes `public.png` but Claude Code's reader is `osascript 'the clipboard as «class PNGf»'` (legacy type) → silent miss ([anthropics/claude-code#30936](https://github.com/anthropics/claude-code/issues/30936), open). Codex (`arboard`) reads `public.png` fine.
  - **Fix direction (works today for both CLIs):** on image drop OR paste over an **image-capable** pane, stage the image to a **temp file** and inject its **ABSOLUTE** path as `@<abspath> ` via the existing `rpc.pty.write`. (a) Add an `IMAGE_CAPABLE_PROVIDERS = Set(['claude','codex'])` gate — precedent: `SLASH_CAPABLE_PROVIDERS` in `insertSkillCommand.ts:18`; `src/shared/providers.ts` has NO capability flag today. (b) In `handleDrop`, branch on `file.type.startsWith('image/')` → stage bytes (reuse `app/src/main/core/design/staging.ts` `DesignStaging.attach` which already accepts `bytesBase64`/`filePath`) → inject absolute path (not the workspace-relative mention, which breaks for out-of-tree screenshots). (c) Add a **capture-phase `paste` listener** on `paneContainerRef` (mirror the existing Cmd+T keydown handler `PaneShell.tsx:192-217`) that, when `clipboardData` has an image, `stopPropagation()`+`preventDefault()`, `getAsFile()` → stage → inject — catching the screenshot before xterm swallows it. (d) Non-image providers / non-image files keep current behavior. AVOID the clipboard-write path (Claude `PNGf` bug). Relevant modules: `PaneShell.tsx:256-321,444`, `insertMention.ts:21`, `terminal-cache.ts:287-291`, `electron/preload.ts:30-35`, `rpc-channels.ts:28`, `rpc-router.ts:986-988`, `registry.ts:386-388`, `providers.ts`, `insertSkillCommand.ts:18`, `design/staging.ts`. Severity: medium. Effort: M.

- 🐞 **[medium] no Copy/Paste on right-click inside a terminal pane** — select text in a pane, right-click → the menu shows Reveal / Open shell / Create worktree… / Stop / Close pane but **no Copy/Paste**, and the selection can't be copied. **Verified root cause (2026-06-10 sweep):** (1) `app/src/renderer/features/command-room/PaneShell.tsx:442-582` — a Radix `<ContextMenuTrigger asChild>` (`:443`) wraps the whole `pane-body` div (`:445-532`, contains `<SessionTerminal>`), so Radix intercepts every `contextmenu` in that subtree and `preventDefault()`s it → xterm's own right-click handler AND the native browser/Electron menu never fire. The 7 `ContextMenuContent` items (`:535` Reveal, `:539` Open shell, `:545` Create worktree, `:553` Open worktree, `:562` Open another workspace, `:570` Stop, `:578` Close pane) simply never included Copy/Paste. (2) No xterm clipboard wiring: `terminal-cache.ts` `buildTerminalOptions` (`:175-196`) sets no `copyOnSelect`/`rightClickSelectsWord`; only Fit/WebLinks/Webgl addons load (`:237-276`); `@xterm/addon-clipboard` is **not** a dep (xterm is `@xterm/xterm ^6.0.0`); nothing calls `getSelection()`/`hasSelection()`. **Fix direction (full wiring plan):** the xterm instance lives in the module-level `cache: Map` (`terminal-cache.ts:129`, `entry.terminal`); add a one-line `getCached(sessionId): CacheEntry | undefined` export (`hasCached` at `:439` exists but doesn't return the entry). In `PaneShell` key on `activeTabId` (`:146`, covers scratch tabs) → add two `ContextMenuItem`s: **Copy** = `getCached(activeTabId)?.terminal.getSelection()` → `navigator.clipboard.writeText` (disable when `!…hasSelection()`); **Paste** = `navigator.clipboard.readText()` → `rpc.pty.write(activeTabId, text)` (disable when PTY exited). `navigator.clipboard.writeText/readText` already works in-renderer with no preload bridge (examples: `ErrorBoundary.tsx:111`, `ProviderInstallModal.tsx:186`, `SkillsTab.tsx:248`); `rpc.pty.write` is the existing keystroke pipe (`terminal-cache.ts:290`, `router-shape.ts:162`, `rpc-channels.ts:28`; `rpc` already imported `PaneShell.tsx:26`). Optional: add `copyOnSelect:true` to `buildTerminalOptions` for the OS-terminal select-to-copy feel. Relevant modules: `PaneShell.tsx:442-582`, `terminal-cache.ts:129/224/287-291/439`, `Terminal.tsx`, `src/components/ui/context-menu.tsx`, `router-shape.ts:162`, `rpc-channels.ts:28`. Severity: medium (core terminal usability gap). Effort: S.
