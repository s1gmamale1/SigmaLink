// V3-W14-007 — The Editor right-rail tab. The placeholder name is kept to
// avoid touching `RightRail.tsx`'s import; the body delegates to the real
// editor component in `features/editor/EditorTab.tsx`.

export { EditorTab as EditorTabPlaceholder } from '@/renderer/features/editor/EditorTab';
