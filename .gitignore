# Dependencies
node_modules/

# Build output
app/dist/
app/electron-dist/
app/release/
app/build/release/
dist/
electron-dist/
release/

# Native module compile artifacts (node-gyp / node-api builds)
app/native/*/build/
app/packages/*/dist/
app/apps/*/dist/

# Packaged installers / signed bundles
*.dmg
*.exe
*.zip
*.blockmap
*.AppImage
*.snap
*.deb
*.rpm
*.msi

# Local databases / runtime data
*.db
*.db-journal
*.db-wal
*.db-shm

# Environment / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Caches
.cache/
.eslintcache
.parcel-cache/
node_modules/.tmp/
.tmp/
*.tsbuildinfo

# yt-dlp scratch (research artifacts kept under docs/)
video.*.vtt
video.*.json

# Python scratch
__pycache__/
*.pyc

# Worktrees + temp data created at runtime
worktrees/

# Test output
coverage/
app/coverage/
playwright-report/
test-results/
docs/06-test/

# Third-party media kept locally for research only — not redistributed
docs/02-research/thumbnails/
docs/02-research/web-images/
docs/02-research/transcripts/*.vtt
docs/02-research/transcripts/*.info.json
docs/02-research/transcripts/*.webp
docs/02-research/transcripts/channel-videos.jsonl
docs/02-research/videos/

# Local orchestration / agent runtime (per-machine, not shared)
.claude-flow/
.claude/
.gemini/
.swarm/
**/.claude-flow/
**/.claude/
**/.gemini/
**/.swarm/
agentdb.rvf
agentdb.rvf.lock
**/agentdb.rvf
**/agentdb.rvf.lock
opencode.json
**/opencode.json

# Ruflo init footprint inside app/ (CLAUDE.md is tracked at repo root only)
app/CLAUDE.md
app/scripts/ruflo-mcp-filter.mjs
app/pnpm-lock.yaml
# NOTE: app/pnpm-workspace.yaml is now TRACKED (v1.7.0) — it holds the
# monorepo workspace `packages:` list (voice-core, bridge-voice, native/*)
# that CI's `pnpm install` needs to resolve @sigmalink/voice-core. It was
# previously a gitignored Ruflo-init footprint (allowBuilds stub only).

# Obsidian vault — per-machine workspace state + plugin data + graph layout.
# The vault content (00-Index.md and the 01..10 numbered notes) are symlinks
# into the project root, so the actual documentation stays tracked. Only the
# vault container directory + .obsidian config + plugin data + Welcome scaffold
# stay local.
SigmaLink/

# BridgeVoice app build output
app/apps/bridge-voice/bridge-dist/
app/apps/bridge-voice/release/
