# SigmaLink — Wishlist (quick capture)

> **Capture inbox.** Jot findings + new ideas here as they land — low ceremony.
> - **Execution sequence** (what we build next, priority-ordered) → `ROADMAP.md`
> - **Shipped record / archive** → `CHANGELOG.md` + `docs/09-release/` + the master
>   memory (`~/.claude/projects/.../memory/`) + Ruflo AgentDB
>
> Flow: capture here → triage into `ROADMAP.md` → on ship, it moves to the archive and
> leaves both working docs.
>
> **Reset 2026-06-08 after v2.0.0 shipped.** The entire v2.0.0 cycle's findings (the whole
> 6-phase roadmap + long-tail + Phases 7–10 + Phase 9 + the resume fixes — **all shipped**)
> were archived to `docs/03-plan/archive/WISHLIST-v2.0.0-cycle.md`; the full record is in
> `CHANGELOG.md` [2.0.0]. Only genuinely-open / forward-looking items are carried below.

---

## 🧊 Deferred XL (held — each is its own cycle; see ROADMAP ADR-003/006)
- **BSP-P4 — Canvas mode** (freeform draggable/resizable panes). XL — pane layout-engine rewrite. Leapfrog if shipped before BridgeCanvas.
- **BSP-P6 — multi-window / dual-window** (detach to its own OS window, multi-monitor). L–XL — multi-`BrowserWindow`. (Phase 10's B2 already shipped the browser-only detach slice.)
- **BSP-P5 — workspaces-as-tabs** (top tab-strip variant of the workspace switcher). S, but a layout-shell change — fold into a future shell pass.
- **Tauri/Rust platform migration.** Evaluated + rejected for now (ADR-006) — the disk leak was a logic bug, not a platform limit. Revisit only if idle-RAM / binary-size become a strategic priority, as its own cycle.

## 🔭 Open follow-ups (post-v2.0.0)
- **[resume] Deterministic codex session capture** — scrape codex's stdout `Session: <uuid>` banner so codex panes resume their OWN session. Today codex panes start fresh each reopen (safe; their cwd disk-scan races in a shared in-place cwd). Claude already resumes via the ghost-heal. See `feedback_inplace_resume_must_be_id_or_fresh`. Effort: M.
- **[resume] CRIT-fU-A — resume self-heal: recreate-if-missing worktree before spawn.** A `running` pane can reference a `worktree_path` that's gone; resume re-spawns without recreating it (recovered live in retest, so deferred). `resume-launcher.ts` recreate-if-missing before spawn. Effort: S.
- **[jorvis] `turnId` on the `ToolTrace` payload** — airtight per-turn inline-chip scoping (today `conversationId`-scoped, bounded by mount lifecycle). Effort: S.
- **[orch] B3 review nits** — SIGKILL escalation, sub-second adopt window, conversation-switch busy-clear. Effort: S.
- **[arch] N1 — `Launcher.tsx` > 500 lines → extract `launch-plan.ts`.** Effort: S.
- **[browser] BSP-B4 — `WebContentsView` input/focus reliability audit** (esp. form fields). Effort: M.
- 🐞 **[low] closing a pane raises a "Pane exited (code 0)" toast for a normal/user-initiated close** — the `pty-exit` notification source fires on EVERY exit, including a deliberate close (`handleRemove` → `rpc.pty.kill` produces a clean `code 0` exit). It can't distinguish "user closed this" from "it exited on its own", so a routine close looks like an error/warning toast (operator screenshot 2026-06-09: "Pane exited (code 0) ×2"). `src/main/core/notifications/sources/pty-exit.ts:47-73` (severity map L58-59; title L68). Fix: thread a "user-initiated" flag through the kill/close path (e.g. mark the session id as intentionally-closing before `rpc.pty.kill`, or pass a reason on the `pty:exit` event) and **suppress the notification for intentional closes**; keep notifications for spontaneous/non-zero exits. Severity: low (cosmetic noise). Effort: S–M.
- ✅ **[BSP tiling] pane resize (box + content) — RESOLVED + MERGED to main `e9ca446` (#133, squash; CI green lint+build / e2e-matrix, Opus-approved) 2026-06-10**. (1) BOX glitch: pivot `BspLayout`→`PaneGrid` fill-grid + re-architect to CSS-Grid driven by CSS custom properties React never writes (`5c3d1bb`) — fixed the React-reconcile stomp of the imperative drag. Operator-confirmed smooth. See `feedback_imperative_drag_stomped_by_react_reconcile`. (2) CONTENT (PTY text) reflow: WebGL renderer (`@xterm/addon-webgl@0.19.0`, `201659c`, `onContextLoss`→DOM fallback) + the text-render agent's keystone fixes — restore `fit.fit()`'s `_renderService.clear()` to kill ghost/duplicated text (`66ffce4`; an earlier split had dropped it) + suppress per-frame RO refit during drag via `sigma:pane-resize-start/-end` to kill the mid-drag SIGWINCH storm (`45a558e`). Operator: *"a bit better but enough"* — good-enough to ship. Opus review: code APPROVED (re-render invariant airtight; no WebGL blank-pane risk). Residual polish (optional, low-pri): live-track content during drag, scrollback reflow cost on column change, font-atlas/glyph caching.
- *(capture new findings here)*

## 🔬 Deep-dive audit — 2026-06-10 (5 read-only agents: main bugs · renderer bugs · perf · win32 · dead code; all evidence-verified at HEAD, WIP excluded)

> ✅ **SHIPPED 2026-06-10/11.** Promoted → root `ROADMAP.md` Phases 4–12 (hotlist #7–12, ADR-004/005/006), executed via 12 TDD plans. **Phases 4–11 all merged + CI-green:** P4 db-bootstrap+reaper (#136/#138), P5 PTY lifecycle (#139), P6 RPC boundary (#140), P7 terminal-cache/scratch (#141), P8 perf hot-paths (#142), P9 perf render/bundle (#143), P10 renderer-state+jorvis (#144/#145), **P11 Windows runtime (#148, Windows-CI verified — incl. the `workspaces.open` npx-auto-download hang fix the new windows-latest CI leg caught).** P4–10 released in **v2.1.0**. **Phase 12 dead-code reassigned to a concurrent agent** (this lane's PR #147 yielded/closed to avoid a delete-collision). Evidence entries below retained for historical reference; permanent record = CHANGELOG + master memory + Ruflo `patterns`.

### Main process — bugs
- 🔴 **CRIT [db] bootstrap resurrects the UNIQUE `workspaces_root_idx` that mig 0034 dropped** — `client.ts:28` BOOTSTRAP_SQL runs every boot (0034 drops once, recorded in schema_migrations) → multi-ws-same-dir re-breaks after one restart; once dup `root_path` rows exist, `CREATE UNIQUE INDEX` throws → **boot fails** (initializeDatabase unwrapped). Fix: non-unique in bootstrap + defensive drop of the unique twin. S
- 🔴 **CRIT [reaper] SF-13 prune fence violates keep⊇use AND cross-workspace isolation** — `workspaces/cleanup.ts:79-90` keeps only `starting|running` of ONE workspace, then rm-rf's everything under the REPO-scoped `worktrees/<repoHash>/` → deletes exited(-1) resume-eligible worktrees + OTHER workspaces' LIVE worktrees (repoHash shared across ws since 0034). Boot sibling `worktree-cleanup.ts:54-72` has the broad fence with the "stay as broad as resume-launcher" comment — this is the drifted twin. Fix: widen to that predicate, drop the ws filter. M
- 🟠 **HIGH [security] `panes.brief` writes to a renderer-supplied fs path, no containment** — `rpc-router.ts:1295` → `writeScopeBlock(worktreePath,…)` mkdir+writes a CLAUDE.md anywhere (prompt-injection vector for any CLI reading it); sibling handlers all validate. Fix: `assertAllowedPath`. S
- 🟠 **HIGH [pty] forget-timer kills a freshly re-created pane** — `registry.ts:326,342`: exit schedules `forget(id)` +3s; recreating the same id inside the window (resume/respawn reuse DB row ids; alive-guard passes on exited-unforgotten records) → the stale timer kills the NEW record ~3s in; also allows concurrent-resume double-spawn zombies. Fix: identity guard (`sessions.get(id)!==rec` at fire time). S
- 🟡 MED [pty] `resume-launcher.ts:270-291` `attachExitPersistence` ignores `isPtyCrash` — 3rd of 3 mirrored exit-classifier sites drifted (launcher.ts:644 + factory-spawn.ts:531 use it) → resumed-pane crashes recorded `exited`, GC'd on restore, never respawn-eligible. S
- 🟡 MED [resume] `resume-launcher.ts:397-446` respawn-fresh never heals/nulls `external_session_id` → next reopen resumes the PRE-crash conversation (ghost-heal exists only on the resume path; mirror it). M
- 🟡 MED [ws] `factory.ts:359-377` `workspaces.remove` deletes only the row — live PTYs keep running headless; `agent_sessions` orphan (no FK cascade); janitor flips them exited(-1) whose worktrees are then kept forever. M
- 🟡 MED [pty] CLI-exit sentinel has no cross-chunk carry — `registry.ts:261` + `sentinel.ts:58`: a sentinel split across two PTY reads is missed (status stalls) + raw marker halves render in the pane; shell-first is the default mode. Fix: ~64-char per-session tail carry (ProtocolLineBuffer is the buffered sibling). M
- 🟡 MED-LOW [notif] assistant `launch_pane` (tools.ts) + sigmabench swarm deps omit the `notifications`/`broadcastPtyError` sinks the router twin threads (C6 class persists off-router) → disk-guard CRIT bell + crash banners silent on Jorvis-dispatched launches. S
- 🟢 LOW — `factory-spawn.ts:136` skips the M1 model allowlist its launcher twin enforces (S) · `rpc-router.ts:585` scrollback sink wired by boot-time IIFE → KV toggle-ON dead at runtime, contradicts in-code contract (S) · `rpc-router.ts:1612` `fs.exists` skips allowedRoots → fs existence oracle (S)
- Mentions: `removeWorkspaceAndGc` prunes before killing live sessions → spared worktrees permanently unreapable (cold-install guard then skips the repo) · `gcScrollback` never reaps `*.log.tmp` · `local-pty.ts:122` defaultShell ignores caller `env.SHELL` on darwin · `pruneRepoDir` rm-rf's stray non-dir entries · exit pane-event kind race on forgotten records.

### Renderer — bugs
- 🟠 **HIGH [panes] scratch-shell tabs leak PTYs + xterm/WebGL cache entries** — `PaneShell.tsx:145-186`: per-mount `scratchTabs` resets on room/ws switch (orphaned PTYs, no UI handle to close); pane close (`CommandRoom.tsx:257`) doesn't kill scratch; `closeScratch` never calls `terminal-cache.destroy()` and the GC (`use-terminal-cache-gc.ts:33`) can't see scratch ids → leaks until 32-entry LRU. Fix: module-scope scratch state keyed by sessionId, kill+destroy on unmount/close. M
- 🟡 MED [rooms] GLOBAL_ROOMS check drifted at 2/4 sibling sites — `state.reducer.ts:276` (`WORKSPACE_OPEN` seeds `'settings'`/`'automations'` into roomByWorkspace) + `use-session-restore.ts:267` fallbackRoom → boot can land in Settings and the workspace's real last room is lost. Fix: share `isGlobalRoom`. S
- 🟡 MED [jorvis] `use-jorvis-pane-events.ts:27` PaneEventStore mutates the snapshot array in place → `useSyncExternalStore` bails on Object.is → pane-event cards render only on unrelated re-renders. Fix: copy-on-add. S
- 🟡 MED [jorvis] `use-jorvis-conversations.ts:92-125` hydrate applies async results with no request-token/alive re-check → late response wins (wrong conversation shown vs KV-persisted; cross-workspace paint). S
- 🟡 MED [panes] `use-prompt-card.ts:63` SIGMA::PROMPT lines arriving while the pane is unmounted are lost (pty-data-bus has no replay) → agent blocks on stdin with no card. Module-scope line buffer per sessionId (splash pattern) or rescan snapshot tail. M (feature opt-in, default OFF)
- 🟢 LOW — `terminal-cache.ts:198` LRU can destroy the on-screen terminal (lastAccessed never bumped on writes; skip attached-to-host victims) (S) · `right-rail/Splitter.tsx:47` body cursor/userSelect not reset on mid-drag unmount (S) · `use-jorvis-jump-to-message.ts:36` single-rAF highlight can precede React commit + uncleared 1.5s classList timer (S) · `usePaneLiveStats.ts:119` + `useSwarmLiveStats.ts:101` overlapping 3s async polls reorder setStats / corrupt tok-s baseline (S) · `use-jorvis-assistant-state.ts:149` setMessages dispatched inside a setStreaming updater (S)
- Mentions: terminal-cache ignores fresh `ctx` on cache hit (frozen wsIdRef, contradicts interface comment) · snapshot drain can double-write bytes also in `pending` (dup text on fast first attach) · Launcher vs Sidebar swarm-hydration drift (swarm-but-no-panes skips UPSERT_SWARM) · composer clear dedups on identical value → typed text survives a banner-retry send · `toggleRail` KV write inside a state updater · `lastSnapshotKeyRef` marked before the 250ms debounce fires (final snapshot droppable on quit) · PaneDivider window listeners leak on mid-drag unmount.

### Perf — optimizations (top 3 user-feelable: ps-storm · recharts bundle · transcript/mailbox memo)
- 🟠 HIGH — `process-tree.ts:47` SYNC `execFileSync('ps…')` ~23ms/call, fired per running pane per 3s via `usePaneLiveStats` → 12 panes ≈ 280ms/3s main-loop blockage → typing/stream hitches under multi-pane load. Fix: ONE shared async `ps` snapshot + 2-3s TTL; panes look up their subtree locally. S/M
- 🟠 MED-HIGH — recharts+d3 ship in the EAGER vendor-react chunk (~450KB excess parse every boot; `vite.config.ts:29` `includes('react')` catches react-smooth) for ONE gear-popover sparkline (`GitActivityStrip.tsx`). Lazy-load or ~30-line SVG bars + fix the chunk matcher. S
- 🟠 MED-HIGH — `MailboxBubble.tsx:87` broad `useAppState()` PER message row → every global dispatch (pane focus, notif, each append) re-renders ALL N bubbles incl. brief-payload parsing. Selector (`activeWorkspace?.id`) + memo. S
- 🟡 MED — `ChatTranscript.tsx:148,283` ChatRow unmemoized + ToolBody re-runs JSON.parse/stringify per historical tool row per render → whole-transcript re-render per stream delta (long-conversation jank). memo + useMemo. S
- 🟡 MED — `state.reducer.ts:528` swarmMessages unbounded (hydrate 200, append forever) + SideChat rebuilds runGroups O(n) per append, unvirtualized. Cap ~500 + window. S/M
- 🟡 MED — `usePaneLiveStats.ts:179` per-pane independent 3s interval (2 RPCs/tick), no `document.hidden` pause → 2N/3s RPCs while occluded (and keeps the ps storm running). Refcounted shared poller + visibility pause (pattern exists ×2 in `src/renderer/lib/`). S
- 🟡 MED — `git-ops.ts:64` gitStatus = 3 git spawns/poll + full filename arrays over IPC where PaneShell needs a COUNT; worktree-per-pane defeats the per-repoPath dedupe (20 panes = 60 spawns/15s). Count-only RPC for the header path + stagger phases. M
- 🟡 MED — remaining broad `useAppState()` in hot mounts: `JorvisRoom.tsx:59` (re-renders transcript per global dispatch — compounds the ChatRow issue), `EditorTab.tsx:88`. PERF-3 continuation. S
- 🟢 LOW-MED — `electron/main.ts:740` sync interactive login-shell PATH probe before createWindow (+200ms-1.5s cold boot on heavy zsh configs): KV-cache PATH + async refresh (M) · terminal-cache attaches WebGL to ALL ≤32 cached terminals vs Chromium's ~16-context cap → silent DOM-renderer fallback can hit VISIBLE panes; load WebGL on attach / dispose on detach (M) · `parsers.ts:39` runRefreshOnEvent no debounce — N `memory:changed` bursts = N full refetches (250ms coalesce fixes 4 channels) (S) · `RufloReadinessPill.tsx:32` broad useAppState in the always-mounted breadcrumb (S)

### Windows (win32) — verdict: structurally aware, NOT runtime-ready. CI-green ≠ providers launch (runners have no CLIs installed)
- 🔴 **CRIT confirmed — cmd.exe-wrap argv re-quoting corrupts EVERY npm `.cmd`-shim launch** — `spawn-cross-platform.ts:31-61` + `windows-spawn.ts:88-96` pass the pre-quoted `/d /s /c <line>` as normal argv → libuv/node-pty re-quote+backslash-escape (cmd.exe doesn't grok `\"`). Consumers: panes (`local-pty.ts:424`), Jorvis (`runClaudeCliTurn.ts:263`), provider probes, `git-ops.ts:417`, review runner — and `claude` on Windows IS `claude.cmd`. Fix: `windowsVerbatimArguments:true` + hand-built command line. M
- 🟠 HIGH confirmed — `http-daemon-supervisor.ts:306,325,376` ruflo/npx tiers raw-`spawn` bare npm shims with `shell:false` → ENOENT (CreateProcessW can't exec `.cmd`) while the `where`-based gate happily selects them → crash-retry loop; only the userData/ELECTRON_RUN_AS_NODE tier works. Route through `spawnExecutable`. S
- 🟡 MED confirmed — `seed-workspace-memory.ts:59` bare `npx` ENOENT, error swallowed → silent no-op (S) · `providers.ts:206` cursor win32 installCommand = `bash -c 'curl … | bash'` (no bash on stock Windows); kimi assumes pip (S) · `rpc-router.ts:915` openShell `cd /d "<path>"` hits the same re-quoting trap (+ caret-escapes wrong inside quotes) → "Open in Terminal" broken for `C:\Users\First Last\…` (S)
- 🟡 MED suspect — `claude-resume-sigma.ts:166` resume bridge prefers symlink (EPERM without Dev Mode) → copyFile fallback that self-documents session divergence; the whole resume/ghost-heal family is un-dogfooded on win32; consider hardlink (M) · `process-tree.ts:42` darwin-only `ps` → no tree-kill on win32 (CLI-spawned MCP/daemon grandchildren leak) + pane RSS stats permanently empty; `taskkill /T /F` + PowerShell CIM (M) · worktree base `AppData\Roaming\SigmaLink\worktrees\<hash>` ≈60 chars before the repo → MAX_PATH risk; set `core.longpaths` on worktree add or shorten base (S) · `rpc-router.ts:1033` scratch shell honors `process.env.SHELL` on win32 (git-bash users export `/usr/bin/bash` → ENOENT) and bypasses `defaultShell()`'s pwsh preference (S)
- 🟢 LOW — janitor `fs.rm` vs win32 open-handle EBUSY/EPERM (retry+backoff, kill-tree first) (S) · `mcp-trust.ts:196` cursor trust runCli bare-spawns what detectCli resolved as `.cmd` (S) · shell-first force-coerced to `direct` on win32 (H-6) → no exit sentinel / no crash-shell fallback, deliberate but user-visible gap (M, post-dogfood) · PTT `CommandOrControl+Alt+Space` = Ctrl+Alt+Space collides with Windows IME toggle on some layouts (S) · NSIS voice natives load never device-verified (device)
- **Test gap (makes the CRIT class CI-visible):** win32 e2e installing a stub `.cmd` shim on PATH + asserting a pane-launch round-trip. M
- Device-only: ConPTY pane feel (resize/scrollback/exit codes) · symlink-vs-copy resume divergence · kill/reap EBUSY cycles · MAX_PATH on a real deep repo · NSIS voice natives. (Feeds W-4.)

### Dead code (~470 committed LOC + 1 unused prod dependency)
- `monaco-editor` dep entirely unused (@monaco-editor/react defaults to its CDN loader; zero imports; vite manualChunks comment misleading) — drop from package.json (S) · `src/shared/events.ts` 116 LOC zero importers (S) · `ui/sheet.tsx` 142 LOC (S) · `ui/separator.tsx` + `@radix-ui/react-separator` dep (sole importer = its own polish-test block) (S) · `hooks/use-mobile.ts` (S) · `ui/skeleton.tsx` (App ships a local RoomSkeleton) (S)
- `PaneHeader.tsx:204` always-false `[data-grid-density=dense]` selector — its setter died with GridLayout; test asserts the dead literal (S) · `PaneShell.tsx:70,91,408` dead `inSplitGroup` prop → `canSplit` always true (S) · `index.css:~622` `.memory-tri-grid` zero consumers (S) · stale GridLayout comments: `CommandRoom.tsx:3` · `use-terminal-cache-gc.ts:32` · `themes.ts:156` · `state.reducer.ts:692` (S)
- `src/main/core/voice/voice-stats.ts` functions runtime-dead — only the `SessionStat` TYPE is imported (VoiceTab.tsx:23); repoint to voice-core's twin + delete file+test (S) · ⚠️ **NOT dead:** the `src/main/core/voice/` tree itself is LIVE (rpc-router:113,122 · electron/main.ts:18,230,250 · VoiceTab) — the real debt is `model-registry.ts`/`voice-stats.ts` DUPLICATION vs `packages/voice-core` (C-10 drift class; consolidation L) · 12 zero-ref exported types in `shared/types.ts` (S-M; verify none are doc anchors) · `scripts/reapply-ruflo-hook-tuning.cjs` one-shot operator script, docs-only refs (keep-or-delete call)

> Cross-cutting: the dominant bug class is again **sibling-call-site drift** (bootstrap vs 0034 · SF-13 vs boot cleanup · 3-way exit classifier · ghost-heal resume-vs-respawn · M1 allowlist · brief vs validated handlers · GLOBAL_ROOMS 2/4 · Launcher vs Sidebar). Suggested first slice: the 2 CRIT main bugs + the win32 CRIT spawn fix + scratch-tab leak + ps-storm.

## 🚧 Operator-owned (blocked on a human / device — non-blocking for the shipped tag)
- **v2.0.0 post-release VISUAL eyeballs:** N1 wizard across themes · N2 browser drag + no-reload-on-reopen · N3 Jorvis live reply (run `claude` once for trust) · PERF-15 swarm-rail under live multi-agent streaming · `npm run test:perf` jank/IPC-rate delta.
- **SF-12 migration `0026`** — data-repair migration dormant pending the operator running diagnostic SQL on a real `agent_sessions` dump first. SQL in `docs/09-release/release-notes-1.35.0.txt`.
- **W-4** — win32 shell-first dogfood (P8–P9). Needs an operator Windows device.
- **FE-4** — voice items + device a11y QA. Behind native voice builds / needs the device.

## 📌 Standing references (not findings — kept for quick lookup)
- **Distribution posture:** internal use only. No signed-distribution paths (EV cert, MS Store, WinGet, Apple Developer, wake-word licensing). Canonical: `app/build/nsis/README — First launch.txt` + `scripts/install-macos.sh`.
- **ADR 2026-05-16 — Linux not supported:** macOS arm64 + Windows x64 only; no Linux CI/smoke/installer/docs. Reversal needs a new ADR.
- **Source ledgers:** `docs/08-bugs/BACKLOG.md` (bugs/optimizations) · `docs/03-plan/V3_PARITY_BACKLOG.md` (V3 parity — resolved v1.5.1, historical) · `docs/02-research/bridgemind-review-2026-05-22/` + `docs/02-research/videos/0NU7O7u-yfM-REVIEW.md` (competitive source) · the full v2.0.0-cycle wishlist → `docs/03-plan/archive/WISHLIST-v2.0.0-cycle.md`.
