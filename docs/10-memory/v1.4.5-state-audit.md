# SigmaLink v1.4.5 — Integrated State Audit

> **Snapshot date:** 2026-05-18
> **Last tagged release:** v1.4.5 (commit `54fb1bb`, published 2026-05-17T19:19:11Z, 11 assets)
> **Branch:** `main` (clean)
> **Audit method:** 3 parallel Explore subagents (Shipped/Architecture, Open/Degraded, Removed/Roadmap) + Opus 4.7 synthesis + manual verification of two discrepancies (proper-lockfile install state, PaneHeader doc-rot).

---

## 1 — Release timeline (v1.4.x line)

6 tagged releases in 52 hours:

| Release | Date | Theme |
|---|---|---|
| v1.4.0 | 2026-05-16 | Sigma Assistant orchestrator (W-2) — session resume, retry-once stale-id fallback, right-rail UI |
| v1.4.1 | 2026-05-16 | Bridge → Sigma rename sweep + Pane→Sigma mailbox back-channel + SigmaRoom 922→283 LOC split |
| v1.4.2 | 2026-05-17 | 10 stability/Windows packets (spawn ENOENT, Settings routing, xterm preservation, NSIS welcome, Pane Focus, rAF resize, disk-scan scoping, shellcheck CI) |
| v1.4.3 | 2026-05-17 | Gemini bridge (`projects.json` alias) + pane rehydration + Pane Split (H/V) + Minimise + EmptyState + worktree dedupe |
| v1.4.4 | 2026-05-18 | 7 reviewer paper cuts + Playwright `navTo` refresh |
| v1.4.5 | 2026-05-18 | proper-lockfile race fix + SessionStep flake closure + factory.ts/runClaudeCliTurn.ts LOC splits |

---

## 2 — Capabilities operating today (verified end-to-end)

| Capability | State | Notes |
|---|---|---|
| Workspace lifecycle (create / open / multi-tab / persist) | ✅ | activeId in kv; restored on restart |
| Per-pane git worktrees | ✅ | `<userData>/SigmaLink/worktrees/<repoHash>/<role>/<task>-<8char>` |
| 5 CLI providers (Claude, Codex, Gemini, Kimi, OpenCode) — pane spawn | ✅ | + shell/custom sentinels |
| Session resume across restart | ⚠️ partial | Claude + Codex ✅; Gemini via `projects.json` alias (drops args on `'missing'`); Kimi unsupported upstream |
| Sigma Assistant orchestrator (turn loop, MCP tool calls, pane mailbox) | ✅ | v1.4.0 resume + v1.4.1 mailbox |
| Ruflo MCP auto-bind (5-CLI canonical args, self-heal user configs) | ✅ | v1.3.5 — canonical args `['-y', '@claude-flow/cli@latest', 'mcp', 'start']` |
| Pane Focus / Split-H / Split-V / Minimise | ✅ | v1.4.2 Focus + v1.4.3 Split/Minimise; max 2-level deep |
| Pane rehydration on workspace open | ✅ | `panes.listForWorkspace` RPC, MAX(started_at) per (workspaceId, paneIndex) |
| Disk-scan session capture (codex/kimi/opencode) | ✅ | v1.2.8 hybrid pre-assign + disk-scan model |
| Auto-update without code signing | ✅ | macOS DMG manual download path; Win `verifyUpdateCodeSignature:false` |
| One-line installers (curl/bash mac, iex/PS Win) | ✅ | macOS arm64 + Windows x64 only |
| Skills fan-out (5-CLI write paths, Zod validation, marketplace) | ✅ | Per-provider toggle in `skill_provider_state` |
| In-app browser (Playwright MCP, npx-on-demand stdio) | ✅ | First call: ~10s npx + 170 MB Chromium download |
| Wikilink memory + force-directed graph | ✅ | MemoryMcpSupervisor in-process MCP, 12 tools |
| Kanban tasks + review/diff/3-way-merge | ✅ | |
| Native voice | ⚠️ partial | macOS Speech.framework ✅; Windows = Web Speech API (cloud-dependent) |
| 17 DB migrations, idempotent, boot-run | ✅ | `runBootJanitor()` before any RPC |

---

## 3 — Operating so-so (works with caveats)

| Item | Caveat | Status |
|---|---|---|
| Gemini resume | `projects.json` alias bridge works, but upstream lacks formal `--resume`; launcher drops resume args on `'missing'` and falls back to fresh spawn | Shipped degraded — P3 |
| Kimi resume | Upstream protocol unverified; launcher skips Kimi from resume path | Indefinite — external dep |
| Kimi MCP fanout | `skills/fanout.ts` hardcodes `[claude, codex, gemini]`; Kimi panes need manual Ruflo MCP config | Out-of-scope per BACKLOG |
| Windows voice | Falls through to Web Speech API → cloud-dependent (Google STT); air-gapped Win = no voice | v1.3 Native SAPI5 binding queued (L ~1wk) |
| macOS x64 one-line install | DMG is built + shipped (~141 MB); `install-macos.sh` line 47-48 rejects with "Only Apple Silicon (arm64) is currently supported" | XS (~30min) — relax arch gate + select correct asset by `uname -m` |
| Linux | electron-builder emits AppImage + .deb locally but no CI / smoke tests / install docs | **ADR 2026-05-16: not supported** |
| Auto-update on Windows | Works but no verification flow shipped to prove signature/skip | S — queued |
| Terminal.tsx snapshot-attach race | 1-5ms IPC drop window during workspace tab swap | XS — queued (R-1.2.7-1) |
| Parchment theme Launch button | Low-contrast (BUG-W7-015) | XS — queued |

---

## 4 — What it CAN'T do yet

| Capability | Why | Plan |
|---|---|---|
| Cross-machine session sync | No transport layer; v1.2.8 declared out-of-scope | L — v1.3+ |
| Notifications + top-right bell | V3-W12 deferred from v1.1.4 | M (~2d) — queued |
| Frameless chrome (`windowsControlsOverlay`) | Not wired | M (~1d) — queued |
| Provider auto-install ("kimi not found → run installer?") | Out-of-scope v1.2.8 | M — queued |
| OpenCode SQLite direct read (skip subprocess) | Out-of-scope v1.2.8; subprocess works | S — queued |
| Apple-notarized macOS DMG | $99/yr Developer ID | Funded-only |
| EV-signed Windows installer | $300-700/yr cert | Funded-only |
| Microsoft Store / WinGet | Reviews + M effort | Funded-only |

---

## 5 — Currently broken / blocking

### 🔴 P1 — Local dev environment needs `pnpm install` refresh (CI unaffected)

`proper-lockfile@^4.1.2` declared in `app/package.json` (v1.4.5) but NOT in local `app/node_modules/`.

- **Confirmed:** `ls /Users/aisigma/projects/SigmaLink/app/node_modules/proper-lockfile` → does not exist
- **Files importing it:** `gemini-resume-bridge.ts:38`, `session-disk-scanner.ts:27`
- **6 vitest suites fail** at import time on this machine
- **CI is unaffected** — release-macos and release-windows both green 2026-05-17 (505 pass / 1 skip)
- **Fix:** `cd app && pnpm install` (≤30s)

### 🔴 P1 — Playwright e2e smoke stale selectors

- `tests/e2e/smoke.spec.ts` references `aria-label="Bridge Assistant"` (should be Sigma), direct Swarm Room nav (moved into `RoomsMenuButton` dropdown)
- v1.4.4 partially refreshed `navTo()` but the full suite isn't green yet
- Effort: M (~1d) — blocks CI smoke

### 🟡 Doc rot — `PaneHeader.tsx` header comment is stale

- Lines 7-12 still say `Split + Minimise are intentional placeholders marked disabled ... pop a "Coming in v1.2" tooltip`
- Actual implementation at line 73+ wires v1.4.3 #06 split + minimise functionally
- Effort: XS (~5min) — fixed in this cleanup batch

### 🟡 v1.4.6 informational

- **opencode-Qwen silent-fail**: first non-interactive `opencode run -m qwen/qwen3-coder-plus` produced 0-byte output; orchestrator skill mitigates via Sonnet fallback; foreground probe + `opencode auth status` check needed (~1hr)

---

## 6 — Removed / dropped over v1.0 → v1.4.5

| Removed | When | Why |
|---|---|---|
| SigmaCode provider stub | v1.2.4 | Never shipped (was `comingSoon` stub) |
| Cursor Agent | v1.2.4 | Registry trim — focus on 5 core CLIs |
| Aider | v1.2.4 | Registry trim |
| Continue | v1.2.4 | Registry trim |
| Droid, Copilot | v1.2.4 | `ProviderId` union stubs removed |
| `playwright-supervisor.ts` (~400 LOC HTTP MCP) | v1.2.6 | Replaced with stdio (`npx -y @playwright/mcp`) |
| `session-id-extractor.ts` (~174 LOC stdout scraper) | v1.2.8 | Replaced with pre-assign UUID + disk-scan |
| `browser.getMcpUrl` RPC | v1.2.6 | Supervisor deletion made it meaningless |
| `RoomChrome.tsx` (-82 LOC) | v1.1.9 | Single caller inlined |
| `PaneStatusStrip.tsx` | v1.1.9 | Content moved into PaneHeader tooltip |
| `bridge.*` KV namespace | v1.4.1 | Renamed to `sigma.*` (Bridge → Sigma sweep) |
| `mcp-stdio` arg | v1.3.5 | Was invalid; canonical `mcp start` fixed |
| Windows ia32 | v1.2.0 | x64 only |
| Linux CI lanes | v1.2.8 | ADR — not a supported platform |

---

## 7 — Quality snapshot

| Metric | Value | Note |
|---|---|---|
| Test files | 64 (54 `app/src` + 8 e2e + 2 cross-platform) | |
| vitest (CI) | 505 pass / 1 skip / 0 fail | Per v1.4.5 release notes |
| vitest (local right now) | 6 fail | `proper-lockfile` not installed locally; run `pnpm install` |
| eslint | 0 errors, 1 warning (`react-hooks/exhaustive-deps` in `use-session-restore.ts:277`) | |
| tsc | clean | |
| LOC >500 offenders remaining | 14 files | Largest: `rpc-router.ts` 1344, test files 1019/639/594. Post-v1.4.5 splits closed factory + runClaudeCliTurn |
| 17 DB migrations | 0001–0017 | Idempotent, runs at boot |

---

## 8 — Architectural inventory

### Main-process modules

`rpc-router.ts`, `core/pty/{registry, local-pty, claude-resume-bridge, gemini-resume-bridge, session-disk-scanner, resume-launcher}.ts`, `core/git/{worktree, git-ops}.ts`, `core/workspaces/{factory, launcher}.ts`, `core/swarms/{factory, factory-add-agent, factory-spawn, controller, mailbox, boards, console-controller, replay}.ts`, `core/assistant/{controller, conversations-controller, runClaudeCliTurn, runClaudeCliTurn.args, mcp-host-bridge, tool-tracer}.ts`, `core/browser/{manager, controller}.ts`, `core/skills/manager.ts`, `core/memory/{manager, mcp-supervisor}.ts`, `core/review/runner.ts`, `core/tasks/manager.ts`, `core/db/{schema, client, migrate, janitor, kv-controller}.ts`, `core/ruflo/{supervisor, proxy}.ts`, `core/voice/adapter.ts`, `core/design/controller.ts`, `core/credentials/store.ts`, `core/fs/controller.ts`.

### Renderer rooms

`workspace-launcher`, `command-room`, `swarm-room`, `operator-console`, `sigma-assistant`, `browser`, `skills`, `memory`, `review`, `tasks`, `settings`, `sidebar`, `top-bar`, `command-palette`, `right-rail`.

### DB tables (24)

`workspaces`, `agent_sessions`, `kv`, `swarms`, `swarm_agents`, `swarm_messages`, `swarm_skills`, `browser_tabs`, `skills`, `skill_provider_state`, `memories`, `memory_links`, `memory_tags`, `tasks`, `task_comments`, `session_review`, `boards`, `conversations`, `messages`, `sigma_pane_events`, `canvases`, `canvas_dispatches`, `swarm_replay_snapshots`, `swarm_origins`.

### Extension points

- **RPC router** — `defineRouter` + `defineController` pattern, namespaced (pty, workspaces, git, providers, swarms, assistant, browser, skills, memory, review, tasks, kv, design, ruflo, voice, fs)
- **IPC events** — `assistant:state`, `assistant:pane-event`, `pty:data`, `pty:exit`, `app:update-available`, `swarm:message`, `swarm:console`
- **Provider registry** — `providers.ts` (5 CLIs + shell sentinel; per-provider resume args, auto-approve, install hints)
- **Sigma Assistant tools** — 50+ tools incl. `monitor_pane` back-channel
- **Skill fan-out paths** — Claude / Codex / Gemini / Kimi / OpenCode native dirs
- **MCP host bridge** — Unix socket (mac/Linux) or named pipe (Win); pane agents dial back for tool dispatch

---

## 9 — Platform support matrix

| Platform | Installer | Auto-update | Voice | Status |
|---|---|---|---|---|
| macOS 13+ (arm64) | DMG (ad-hoc signed) | electron-updater + manual DMG download | native macOS Speech.framework | Supported; notarisation deferred |
| macOS (x64) | DMG + zip **built + shipped** (~141 MB DMG) | electron-updater via `latest-mac.yml` | — (Speech.framework works on Intel) | **Asset exists; one-line installer rejects (line 47-48 of `install-macos.sh`)** — manual DMG download required |
| Windows 10/11 (x64) | NSIS EXE (unsigned) | electron-updater; `verifyUpdateCodeSignature:false` | Web Speech API (cloud-dependent) | Supported; SmartScreen documented |
| Windows (ia32) | — | — | — | Dropped v1.2.0 |
| Linux (x64) | AppImage + .deb (built, not gated) | — | — | **Not supported** (ADR 2026-05-16) |

---

## 10 — Recommended next moves

In order of effort/impact:

1. **`cd app && pnpm install`** (≤30s) — unblocks local vitest
2. **`PaneHeader.tsx` doc-rot fix** (~5min) — header comment lies about Split/Minimise
3. **WISHLIST.md "Suggested grouping" cleanup** (~10min) — section dates to 2026-05-16, references shipped work
4. **Archive `v1.4.2-bundle/` + `v1.4.3-bundle/`** to `docs/03-plan/archive/`
5. **Pick v1.4.6 theme**:
   - **CI hardening** (~1d): Playwright e2e refresh + cache-dep-path + vitest thresholds + opencode-Qwen probe
   - **Polish bugs** (~½d): Terminal mount race + Parchment contrast
   - **Feature**: Notifications + bell (~2d) OR x64 mac DMG (~2hr quick win)

---

## 11 — Source audits

This synthesis is built from three parallel Explore subagents (all Sonnet, all background, all read-only):

- **Shipped/Architecture audit** — 270s / 78 tool uses → §2, §8
- **Open/Degraded audit** — 378s / 55 tool uses → §3, §5, §7
- **Removed/Roadmap audit** — 136s / 26 tool uses → §4, §6, §9

Plus manual verification of:
- `proper-lockfile` install state — `ls app/node_modules/proper-lockfile` confirmed missing
- `PaneHeader.tsx` doc-rot — `grep` lines 7-12 confirmed stale

Cross-doc sources:
- `CHANGELOG.md` (v1.0.0 → v1.4.5)
- `docs/03-plan/WISHLIST.md` (synced 2026-05-18)
- `docs/08-bugs/{OPEN, BACKLOG}.md`
- `docs/10-memory/master_memory.md` (Phase 1–31)
- `~/.claude/skills/orchestrator/SKILL.md` (v1.4.5 silent-fail maintenance note)
