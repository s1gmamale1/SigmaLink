# V3 Frame Walk — Chapter C (frames 0369-0553, ~720-1085s)
Walker: v3-walker-c

## Narrative arc (≤200 words)

Chapter C opens mid-Design-Tool demo (`0369.jpg`): an HTML element on the live BridgeMind landing page is selected ("The Toolkit" pill) and Claude is editing its containing div. A per-prompt provider picker is visible (`0380.jpg`) listing **Claude / Codex / Gemini / OpenCode**. The user drags an MP4 into the prompt — the path appears verbatim in the Bridge prompt thread (`0398.jpg`) — and the agent both swaps in the marketing video and removes the eyebrow pill (`0405.jpg`). Around frame 0420 the right-hand utility tab flips from `Browser` to `Editor`: a real file tree (left) plus a syntax-highlighted code view of `KanbanAnimation.tsx`, then `RouteGuard.tsx` (`0420.jpg` / `0430.jpg`). Frame 0440 boots the iOS Simulator: the AI-generated **BridgeSpace mobile app** runs the welcome screen, a sign-in chooser (Google / Apple / email) (`0445.jpg`), and a 6-tile dashboard — Terminal, Kanban, Workspace, Swarm, **Canvas**, **Bridge** — with a Pro plan card (`0455.jpg`). The video then cuts to talking-head milestone copy ($90k ARR, day 167) before landing on `localhost:3000/pricing` (`0500.jpg`/`0510.jpg`/`0515.jpg`): a 3-tier plan grid (Basic / Pro / Ultra, Monthly/Annual toggle, "MOST POPULAR" badge on Pro). Frame 0520 surfaces the **BridgeVoice desktop app** with its own sidebar (Overview / History / Dictionary / Instructions / Shortcuts / Subscription / Settings / Changelog) and lifetime stats (153,971 words, 14h 46m, 5,680 sessions, 174 wpm). Outro is talking-head only.

## Confirmed BridgeSpace V3 features (mapped to current SigmaLink)

| # | Feature | Mark | Frame range | Transcript lines | SigmaLink status | Build implication |
|---|---|---|---|---|---|---|
| C-1 | **Per-prompt agent picker in Design Mode** (Claude / Codex / Gemini / OpenCode dropdown attached to a chat box on top of a selected DOM element) | [V] | `0380.jpg` | 340-341 | **missing** — no design-mode entry point in `app/src/renderer/features/browser/`; only `BrowserRoom.tsx`, `AddressBar.tsx`, `TabStrip.tsx` exist | Add an "inspect" overlay to `BrowserRoom` plus a chat dock with a provider chooser routed via `main/core/providers`. |
| C-2 | **Drag-and-drop asset into Design-Mode prompt** (MP4 path injected verbatim into the prompt thread) | [T+V] | `0398.jpg`, `0405.jpg` | 344-372 | **missing** — no file-drop pipeline into terminal/agent prompts found | When a file is dropped, write it to a workspace-scoped staging dir and append the absolute path to the prompt buffer. |
| C-3 | **Live-DOM patch applied back to dev server after agent edit** ("remove this" → Toolkit pill disappears in the iframe; `0405.jpg` shows the patched section in the right-pane render) | [T+V] | `0405.jpg` | 360-373 | **missing** | Requires a Vite HMR / file-watcher loop tied to whichever workspace is bound to the browser tab. |
| C-4 | **Built-in IDE tab next to Browser/Bridge** (right-pane tab strip `Browser \| Editor \| Bridge` visible in `0420.jpg`/`0430.jpg`) | [V] | `0420.jpg`, `0430.jpg` | 376-403 | **missing** — no `Editor` tab in `renderer/features/browser` or elsewhere | Add an `editor` route inside the right-pane tab container; back it with the same FS that the terminals already see. |
| C-5 | **File-tree sidebar inside the IDE** (alphabetical project listing: `agent-discord`, `bridge-battle`, `bridge-legion`, `bridge-space-app`, `bridgeagent`, `bridgebench`, `bridgecode`, … plus deeper files like `ADMIN_ENHANCEMENT_PLAN.md`) | [V] | `0420.jpg` | 380-403 | **missing** | Reuse Electron's `dialog`/`fs` from `app/electron/main.ts` and render a virtualised tree. |
| C-6 | **Click-to-open from terminal output to IDE tab** (transcript: paths printed by Claude become hyperlinks that open in the embedded editor) | [T] | n/a (talking-head over terminal) | 380-403 | **missing** | Parse terminal output for absolute paths; emit IPC to focus the editor pane on that file. |
| C-7 | **Syntax-highlighted code viewer** with TypeScript/JSX colourisation (`0420.jpg` shows `'use client'`, imports from `react`, `framer-motion`, `lucide-react`; `0430.jpg` shows hooks in `RouteGuard.tsx`) | [V] | `0420.jpg`, `0430.jpg` | n/a | **missing** | Drop in Monaco or CodeMirror 6 inside the new editor tab; theme to match the dark UI. |
| C-8 | **AI-generated BridgeSpace mobile app — Welcome screen** ("Build the Future. The vibe coding platform for builders who ship at the speed of thought." + "Start Shipping" CTA + caption "NATURAL LANGUAGE IN — PRODUCTION CODE OUT") rendered in iOS Simulator (`0440.jpg`) | [T+V] | `0440.jpg` | 404-410 | **divergent** — SigmaLink ships only a desktop Electron renderer; no React Native / Expo app exists | Future-track. Mirror the marketing copy in any companion app shell so the welcome surface matches V3. |
| C-9 | **Mobile sign-in screen** with three providers: `Continue with Google`, `Continue with Apple`, `Continue with email` (`0445.jpg`) | [V] | `0445.jpg` | 411-415 | **missing** | If/when the companion app is built it must support all three; today no auth surface exists in `app/src`. |
| C-10 | **Mobile dashboard with 6 quick-action tiles**: Terminal, Kanban, Workspace, Swarm, **Canvas**, **Bridge** — plus a "Pro" plan card showing renewal date (`0455.jpg`) | [V] | `0455.jpg` | 416-423 | **partial** — desktop has Workspace and Swarm rooms (`renderer/features/workspace-launcher`, `swarm-room`); Kanban, Canvas, and Bridge tiles have no equivalent | Treat this tile set as the canonical V3 product surface. Build dedicated rooms for **Kanban**, **Canvas**, and **Bridge** even on desktop. |
| C-11 | **Bridge Canvas as a first-class room** (mobile tile labelled "Canvas — Spatial agent canvas" in `0455.jpg`; not deeply explored in this video but proven shipped) | [V] | `0455.jpg` | n/a (visual only) | **missing** — no `canvas` feature dir exists; `app/src/renderer/features` lacks any canvas surface | Stand up `renderer/features/canvas-room/` matching PRODUCT_SPEC §2.3. |
| C-12 | **Bridge agent surfaced as a discrete tile** ("Bridge — Assistant side panel" in `0455.jpg`) | [V] | `0455.jpg` | 175 (echoed) | **missing** — Bridge agent right-hand panel not present in renderer features | Add a `Bridge` panel/tab to the right-pane container; same provider routing as terminals. |
| C-13 | **Pricing page with three tiers** at `localhost:3000/pricing`: Basic **$20** ($16 annual / SAVE 20%), Pro **$50** ($40), Ultra **$100** ($80); "MOST POPULAR" badge on Pro; trust strip `JOIN 8,500+ BUILDERS`; bottom row `Cancel anytime · 7-day money-back guarantee · Instant access to every product · LOCKED AT TODAY'S RATE` (`0500.jpg`/`0510.jpg`/`0515.jpg`) | [V] | `0500.jpg`, `0510.jpg`, `0515.jpg` | 458-481 | **divergent** — this is a marketing page on bridgemind.ai; SigmaLink is the desktop app, not the website. No pricing surface in `app/src` | Track separately (likely outside the desktop app); however, see C-15 for plan-feature parity needed in-app. |
| C-14 | **Monthly / Annual billing toggle** with a `-20%` chip on Annual (`0500.jpg` Monthly active; `0510.jpg`/`0515.jpg` Annual active and `SAVE 20%` chips appear under each price) | [V] | `0500.jpg` vs `0510.jpg` | 461-466 | **missing** | If a pricing surface is added, mirror this toggle. |
| C-15 | **Plan feature lists** (visible per-tier checklist): Basic = `5,000 credits/month · BridgeMind account access · BridgeSpace (ADE) · Multi-agent swarms · BridgeJarvis voice assistant · Email support`; Pro = `12,500 credits/month · Everything in Basic · BridgeMCP · BridgeVoice · BridgeCode (Coming Soon) · Priority support · Early access`; Ultra = `25,000 credits/month · Everything in Pro · Highest AI model ceilings · Priority AI model routing · Dedicated support · Team seats (coming soon)` (`0500.jpg`/`0510.jpg`) | [V] | `0500.jpg`, `0510.jpg` | 468-481 | **missing in-app**; informs which capabilities should exist | Treat as the master capability matrix. Notable new SKUs: **BridgeMCP**, **BridgeVoice**, **BridgeCode (coming soon)**, **BridgeJarvis voice assistant**. |
| C-16 | **BridgeJarvis voice assistant** (named in the Basic plan's feature list — the wake-word agent shown earlier in the video; same pane shows `"Jarvis, spin up a 4-terminal workspace with Claude."` waveform in `0410.jpg`/`0440.jpg`) | [T+V] | `0410.jpg`, `0440.jpg`, `0500.jpg` | 472-475 | **missing** — no Jarvis/wake-word surface; SigmaLink has `command-palette` but no voice intake | Add a voice-intake module wired to `main/core/skills`. |
| C-17 | **BridgeVoice desktop app** — separate Electron-style window (traffic-light buttons), title `BridgeVoice`, left sidebar items: Overview · History · Dictionary · Instructions · Shortcuts · Subscription (badge) · Settings · Changelog. Right pane "Overview" panel with cards `TOTAL WORDS 153,971`, `SPEAKING TIME 14h 46m`, `SESSIONS 5,680`, `AVG PACE 174 wpm`, plus a "Recent Activity" list of the user's last three transcribed prompts. Account row at lower-left: `matthewmiller87… Pro Plan` (`0520.jpg`) | [V] | `0520.jpg` | 478-481 | **missing** — out of SigmaLink's scope (it's a sibling app), but it confirms a voice-to-text companion is part of V3 | Future-track. If integrated, expose as a skill provider. |
| C-18 | **Live render of BridgeMind landing page hero** showing copy `Build the Future.` + `The vibe coding platform for builders who ship at the speed of thought.` + CTAs `Start Shipping →` and `Join Discord`, plus stats strip `8,500+ DISCORD MEMBERS · 63,000+ YOUTUBE SUBSCRIBERS · 28,000+ X FOLLOWERS` (`0369.jpg`/`0395.jpg`) | [V] | `0369.jpg`, `0395.jpg`, `0410.jpg` | n/a | **divergent** — marketing site copy; not in the desktop app | Reference for any in-app onboarding hero. |
| C-19 | **Live "vibe-mode" demo card** below the hero (`0395.jpg`/`0410.jpg`/`0440.jpg`): green `LIVE DEMO` chip, console-style log: `bridgespace · vibe mode activated · 16 agents ready · "add stripe checkout with usage billing" · 3 agents planning · ✓ Coordinator: task graph (4 subtasks) · ✓ Scout: found src/lib/billing.ts · ✓ Builder: generating 4 file changes · ● shipped · 42s · 0 errors` | [V] | `0395.jpg`, `0410.jpg`, `0440.jpg` | n/a | **partial** — `swarm-room` exists in `app/src/renderer/features/swarm-room/` but no public-facing live-demo widget | Optional: add a marketing-flavoured "live demo" surface; not a desktop-app priority. |

## Frame-by-frame log (sampled)

- **0369.jpg** (~720s) [V]: Design Mode active. Header pill `Click an element in the preview`. Selected element: `span.font-mono.text-[10px]` ("The Toolkit"). Bridge prompt: `"I want you to review this and gain a complete understanding of the div element here in the animation that's inside of it."` Right pane = browser at `localhost:3000` rendering BridgeMind landing page. Lower-right pane = "vibe-mode" live demo card.
- **0375.jpg** (~733s) [V]: Same layout; prompt has been submitted; Claude in middle pane is mid-response with an HTML structure walkthrough ("Container · Structure (3 Layers) · Ambient glow · Terminal card · Body · Caption below · Purpose").
- **0380.jpg** (~743s) [V]: **Per-prompt agent picker** open above the design-mode prompt box. Visible options: `Claude` (selected, marked default), `Codex` (Anthropic Claude 4.6 [shows "Anthropic Claude Code 1"]), `Gemini` (Google Gemini CLI 1), `OpenCode` (OpenCode CLI 1). Modifier hint: `SHIFT + CLICK to select multiple` and `ALT + CLICK to deselect`. Confirms multi-provider routing per prompt.
- **0385.jpg** (~754s) [V]: Patch landed on file `bridgemind-ui/src/app/(public)/sections/AgenticOrganization.tsx` line 376; diff visible: removed the `eyebrow` prop value `"The Toolkit"`. Below: recap "you asked me to remove (...) eyebrow label."
- **0395.jpg** / **0398.jpg** (~787s) [T+V]: Drag-and-drop video in progress. Prompt buffer now contains the absolute path `'/Users/matthewmiller/Desktop/bridgemind/bridgespace-v3.mp4'` followed by the user instruction `"I now want you to replace that animation with this marketing video in the project."` The agent runs `Searching for 1 pattern, reading 1 file, listing 1 directories, running 1 bash command…` and references `bridgespace-v3.webp` / `marketing-v3.mp4`.
- **0405.jpg** (~810s) [V]: Diff applied to `MarketingHero` component lines 295-363; the right-pane render no longer shows the "The Toolkit" pill (visual confirmation of C-3).
- **0410.jpg** (~820s) [V]: Right-pane tab strip is now visible at the very top: `Browser` (selected) · `Editor` · `Bridge`. Vibe-mode demo card still in lower-right.
- **0420.jpg** (~840s) [V]: `Editor` tab now active. Two-column IDE: left = file-tree (alphabetical from `agent-discord` through `tallwind`), right = `KanbanAnimation.tsx` open with TS/JSX syntax highlighting, line numbers, imports (`react`, `framer-motion`, `lucide-react`), `AGENTS` / `COLUMNS` / `TASKS` arrays.
- **0430.jpg** (~860s) [V]: Same IDE; user has clicked a different file path → editor now shows `RouteGuard.tsx` with `useEffect`, `useMemo`, auth-guard imports, `RouteGuardProps` interface.
- **0440.jpg** (~880s) [V]: macOS menubar reads `Simulator`. iOS Simulator window pinned over the BridgeSpace UI. Inside: iPhone 16 Plus shell, status bar `6:27`, BridgeMind wordmark, hero copy `Build the Future.` + `The vibe coding platform for builders who ship at the speed of thought.`, button `Start Shipping →`, footer caption `NATURAL LANGUAGE IN — PRODUCTION CODE OUT`.
- **0445.jpg** (~895s) [V]: Mobile **sign-in chooser**: BridgeMind logo, headline `Welcome back.`, subtext `Connect your BridgeMind account to command your desktop workspace.`, three buttons `Continue with Google`, `Continue with Apple`, `Continue with email`.
- **0455.jpg** (~915s) [V]: Mobile **dashboard** post-login. Header: BridgeMind logo + `Sign out`. Section `DASHBOARD` heading `Welcome back.` and email `bridgemindapps@gmail.com`. Plan card: `Pro · ACTIVE · Renews 2/11/2026`. `Actions` grid (2 columns × 3 rows, badge `6 available`): **Terminal** ("Focus the desktop terminal"), **Kanban** ("Run work from the board"), **Workspace** ("Start a fresh workspace"), **Swarm** ("Multi-agent workspace"), **Canvas** ("Spatial agent canvas"), **Bridge** ("Assistant side panel").
- **0470.jpg** / **0485.jpg** / **0490.jpg** (~940-980s) [T]: Talking-head milestone segment ($90k ARR, 167 days). No new UI.
- **0500.jpg** (~1000s) [V]: BridgeMind website at `/pricing`. Top nav `Pricing · Ecosystem · Community · Company` plus `Log In` / `Get Started`. Heading `Ship at the speed of thought`. Toggle `Monthly | Annual -20%` (Monthly selected). Three plan cards as in C-13/C-15.
- **0510.jpg** / **0515.jpg** (~1020s) [V]: Same page, Annual toggle on; prices update to $16 / $40 / $80 with `SAVE 20%` chips and original prices struck through.
- **0520.jpg** (~1040s) [V]: BridgeVoice desktop window (see C-17 for full sidebar inventory).
- **0530.jpg–0553.jpg** (~1060-1085s) [T]: Closing talking-head + outro. No new UI.

## Open questions

1. **No frame in this chapter shows the `V3` coupon code being typed at checkout.** Transcript (lines 461-466) says `code V3 at checkout`, but the visible pricing pages show no coupon input field. → Is the coupon redeemed via URL param (`?coupon=V3`), Stripe Promotion Code at checkout (next page), or auto-applied? Worth pulling the actual checkout page if SigmaLink ever gates by plan.
2. **Bridge Canvas tile in `0455.jpg` is the only visual proof** that Canvas exists in V3. The video never opens a Canvas room. The tile copy ("Spatial agent canvas") matches PRODUCT_SPEC §2.3, but no UI specifics (zoom, pan, node types, agent attachment) are visible anywhere in chapters A/B/C. → A separate video pull or live-app screenshot is needed before building.
3. **Bridge tile vs. Bridge right-pane tab.** `0410.jpg` shows `Bridge` as a top-right tab in the desktop IDE, while `0455.jpg` shows `Bridge` as a mobile dashboard tile labelled "Assistant side panel". Same agent, two surfaces? Or the mobile tile launches the desktop's Bridge panel? Architecture decision needed.
4. **Editor file-tree depth.** `0420.jpg` lists ~30 top-level entries plus `.md` files under what looks like a single project root (`bridgemind`). Is the IDE pinned to the workspace `cd` directory, or does it expose multiple project roots? No frame shows multi-root.
5. **BridgeMCP / BridgeVoice / BridgeCode are listed as separate Pro/Ultra entitlements** in `0500.jpg`/`0510.jpg`. Only BridgeVoice has a confirmed UI (`0520.jpg`). What does **BridgeMCP** look like — is it an MCP-server marketplace inside BridgeSpace, an admin panel, or just an entitlement flag? Missing from the video.
6. **`63,000+ YOUTUBE SUBSCRIBERS` and `28,000+ X FOLLOWERS`** in the live-demo screenshots conflict with chapter A's earlier numbers — live data, harmless, but worth a cross-walker note.

/Users/aisigma/projects/SigmaLink/docs/02-research/v3/v3-frames-chapter-c.md
